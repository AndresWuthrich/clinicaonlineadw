import { SaludoDirective } from './saludo.directive';

describe('SaludoDirective', () => {
  it('should create an instance', () => {
    const directive = new SaludoDirective();
    expect(directive).toBeTruthy();
  });
});
